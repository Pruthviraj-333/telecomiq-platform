name: Frontend CI

on:
  push:
    branches: [main]
    paths: ['telecomIQ_frontend/**']
  pull_request:
    branches: [main]
    paths: ['telecomIQ_frontend/**']

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: telecomIQ_frontend/package-lock.json

      - name: Install Dependencies
        run: npm ci
        working-directory: telecomIQ_frontend

      - name: Build
        run: npm run build -- --configuration=production
        working-directory: telecomIQ_frontend

      - name: Build Docker Image
        run: docker build -f telecomIQ_frontend/Dockerfile -t telecomiq-frontend:${{ github.sha }} ./telecomIQ_frontend

